#include <iostream>

struct Ponto{
    int x, y;
};

int main(){
    Ponto p;
    int b;
    cin >> p.x >> p.y >> std::endl;
    cin >> b >> std::endl;
    struct Ponto pontos[b];
    
    for (int i=0; i<x ; i++){
        cin >> p.x >> p.y >> pontos[i].Ponto >> std::endl;

    }
}